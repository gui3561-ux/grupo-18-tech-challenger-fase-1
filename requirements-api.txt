# API Core - FastAPI e servidor
fastapi==0.115.0
uvicorn[standard]==0.34.0
pydantic==2.9.2
pydantic-settings==2.7.0

# Machine Learning - PyTorch CPU only (otimizado para Azure)
torch==2.5.0+cpu
--index-url https://download.pytorch.org/whl/cpu

# Data Processing
pandas==2.2.3
numpy==1.26.4
scikit-learn==1.6.1
joblib==1.4.2

# Feature Engineering (apenas joblib para carga do modelo)
# imbalanced-learn não é necessário em produção, apenas no treinamento

# Monitoring e Observability
prometheus-client==0.21.0
structlog==25.5.0

# Nota: Para produção no Azure, use requirements-azure.txt que é gerado
# automaticamente durante o deploy com otimizações específicas
